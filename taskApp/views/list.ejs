<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('header') %>
    <h1><%= title %></h1>
    <p id="msg"><%= message %></p>
    <table class="table">
        <thead>
            <th>name</th>
            <th>category</th>
        </thead>
        <tbody>
            <% tasks.forEach(element => { %>
             <tr>
                <td><%= element.name %></td>
                <td><%= element.category %></td>
                <td><a href="/content/<%= element.id %>">詳細</a></td>
             </tr>
            <% }) %>
        </tbody>
    </table>
    <%- include('footer') %>
</body>
</html>
